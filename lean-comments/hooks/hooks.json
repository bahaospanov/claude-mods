{
  "description": "limit-edits flags comment-heavy Write/Edit calls; limit-turns records each worktree's baseline on first touch and checks the turn's diff when it ends",
  "modules": ["./register.ts"]
}
